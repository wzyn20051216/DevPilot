{
  "id": "request_timeout",
  "title": "实现可校验的请求超时选项",
  "description": "请求超时需要在 schema、配置和客户端构造流程之间正确传递并校验。",
  "difficulty": "hard",
  "category": "cross_module",
  "tags": ["multi_file", "validation", "backward_compatibility"],
  "repo_fixture": "request_timeout_repo",
  "task": "实现 RequestOptions 的可选 timeout：未提供时使用 DEFAULT_TIMEOUT，提供时仅允许 1 到 MAX_TIMEOUT，并让 create_client() 把最终值传给 ApiClient。保持 create_client() 无参数调用兼容。不要修改测试文件。",
  "verification_target": "tests/test_service.py",
  "timeout_seconds": 120
}
